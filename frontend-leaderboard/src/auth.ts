export async function createSmartAccount(): Promise<void> {
  // Stub: simulate async smart account creation
  return new Promise((resolve) => {
    setTimeout(() => {
      console.log("Smart account created!");
      resolve();
    }, 1200);
  });
}